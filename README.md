# sql-leetcode-SELECT-
Part 1 of the LeetCode SQL 50 focuses on mastering basic SELECT queries. This section covers retrieving single and multiple columns, filtering data using WHERE, removing duplicates with DISTINCT, and applying simple conditions. It builds a strong foundation in SQL data retrieval and query logic.
